apiVersion: v1
kind: Service
metadata:
  name: bookstore-app
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: bookstore-app
  ports:
    - port: {{ .Values.app.port }}
      targetPort: {{ .Values.app.port }}
  type: ClusterIP