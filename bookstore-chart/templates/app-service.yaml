apiVersion: v1
kind: Service
metadata:
  name: bookstore-app
  namespace: {{ .Values.global.namespace }}
  labels:
    app: bookstore-app
spec:
  selector:
    app: bookstore-app
  ports:
    - name: http
      port: {{ .Values.app.port }}
      targetPort: {{ .Values.app.port }}
  type: ClusterIP