apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: bookstore-app-monitor
  namespace: bookstore
spec:
  selector:
    matchLabels:
      app: bookstore-app
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s